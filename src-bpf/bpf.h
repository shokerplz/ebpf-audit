#define AF_INET 2

#define MAX_PATH_LEN 128

/// @description "Enable printing to /sys/kernel/debug/tracing/trace_pipe"
const volatile int ENABLE_DEBUG = 1;

/// @description "PID to filter"
const volatile int PID_TARGET = 0;
